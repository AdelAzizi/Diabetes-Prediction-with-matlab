# توضیح فایل `analyze_data.m`

این تابع برای **تحلیل تصویری و آماری داده‌ها** استفاده می‌شود.  
هیچ چیزی را در دیتاست تغییر نمی‌دهد، فقط چند نمودار می‌کشد تا با چشم انسان بتوانیم الگوها را بهتر ببینیم.

---

## ورودی و خروجی

- **ورودی (`data`)**: جدول داده‌ها که ستون‌های آن ویژگی‌ها و ستون آخر معمولاً `Outcome` است.
- **خروجی:**  
  - خروجی مستقیم (متغیر) برنمی‌گرداند، اما چند پنجره‌ی شکل (Figure) باز می‌کند و نمودارها را نشان می‌دهد.

---

## مرحله‌به‌مرحله چه کار می‌کند؟

### ۱. نمایش خلاصه آماری
خلاصه آماری داده‌ها (میانگین، انحراف معیار، کمینه، بیشینه و ...) در پنجره کامند نمایش داده می‌شود.

### ۲. رسم هیستوگرام ویژگی‌ها
برای هر ویژگی یک نمودار هیستوگرام رسم می‌شود که نشان می‌دهد داده‌ها چطور توزیع شده‌اند.

### ۳. رسم ماتریس همبستگی
یک نقشه حرارتی (Heatmap) رسم می‌شود که نشان می‌دهد کدام ویژگی‌ها با هم رابطه قوی‌تری دارند.

---

## توضیح خط به خط کد

```matlab
function analyze_data(data)
```
**خط ۱:** تعریف تابع. نام تابع `analyze_data` است. یک ورودی (`data`) می‌گیرد و خروجی برنمی‌گرداند (چون فقط نمودار می‌کشد).

```matlab
    % ANALYZE_DATA تحلیل آماری و بصری سریع داده‌ها
    
    disp('-----------------------------------------');
    disp('📊 مرحله ۰: تحلیل اکتشافی داده‌ها (EDA)');
    disp('-----------------------------------------');
```
**خط ۲-۶:** کامنت توضیحی و نمایش یک خط جداکننده و عنوان مرحله.

```matlab
    %% ۱. نمایش خلاصه آماری
    % نمایش میانگین، انحراف معیار و... در کامند ویندو
    summary(data);
```
**خط ۸-۱۰:** 
- `summary(data)` خلاصه آماری کامل جدول را نمایش می‌دهد.
- شامل: تعداد داده‌ها، میانگین، انحراف معیار، کمینه، بیشینه و ... برای هر ستون.

```matlab
    %% ۲. رسم نمودار توزیع (Histograms)
    figure('Name', 'Data Distribution', 'Color', 'w');
```
**خط ۱۲-۱۳:** 
- `figure` یک پنجره جدید برای رسم نمودار می‌سازد.
- `'Name'` عنوان پنجره را تنظیم می‌کند.
- `'Color', 'w'` پس‌زمینه را سفید می‌کند.

```matlab
    features = data.Properties.VariableNames(1:end-1);
```
**خط ۱۴:** 
- `data.Properties.VariableNames` لیست نام همه ستون‌های جدول را برمی‌گرداند.
- `(1:end-1)` یعنی همه ستون‌ها به جز آخرین (چون ستون آخر `Outcome` است و ما فقط ویژگی‌ها را می‌خواهیم).

```matlab
    for i = 1:length(features)
```
**خط ۱۶:** یک حلقه شروع می‌شود که برای هر ویژگی اجرا می‌شود.

```matlab
        subplot(3, 3, i);
```
**خط ۱۷:** 
- `subplot(3, 3, i)` یک شبکه ۳×۳ می‌سازد و نمودار iام را در یکی از خانه‌های این شبکه قرار می‌دهد.
- یعنی ۹ نمودار کوچک در یک پنجره بزرگ.

```matlab
        histogram(data{:, i}, 'FaceColor', [0.2, 0.6, 0.8]);
```
**خط ۱۸:** 
- `histogram` یک نمودار هیستوگرام می‌کشد.
- `data{:, i}` یعنی همه سطرها و ستون iام.
- `'FaceColor', [0.2, 0.6, 0.8]` رنگ نمودار را آبی روشن می‌کند (RGB).

```matlab
        title(features{i});
```
**خط ۱۹:** عنوان نمودار را نام ویژگی قرار می‌دهد.

```matlab
        axis tight;
```
**خط ۲۰:** محورها را طوری تنظیم می‌کند که دقیقاً دور داده‌ها بکشد (بدون فضای اضافی).

```matlab
    end
```
**خط ۲۱:** پایان حلقه `for`.

```matlab
    sgtitle('توزیع ویژگی‌ها (قبل از پیش‌پردازش)');
```
**خط ۲۲:** یک عنوان کلی برای همه نمودارهای داخل پنجره قرار می‌دهد.

```matlab
    drawnow; % رسم فوری
```
**خط ۲۳:** 
- `drawnow` نمودارها را فوراً رسم می‌کند (بدون منتظر ماندن).
- بدون این دستور، ممکن است نمودارها تا پایان تابع نمایش داده نشوند.

```matlab
    %% ۳. رسم ماتریس همبستگی (Correlation)
    figure('Name', 'Correlation Matrix', 'Color', 'w');
```
**خط ۲۵-۲۶:** یک پنجره جدید برای نمودار همبستگی می‌سازد.

```matlab
    % محاسبه فقط برای ستون‌های عددی
    corrMatrix = corr(data{:, 1:end});
```
**خط ۲۷-۲۸:** 
- `corr` ماتریس همبستگی را محاسبه می‌کند.
- `data{:, 1:end}` یعنی همه ستون‌ها (شامل `Outcome` هم).
- نتیجه یک ماتریس مربعی است که هر خانه آن ضریب همبستگی بین دو ستون است (از -۱ تا ۱).

```matlab
    heatmap(data.Properties.VariableNames, data.Properties.VariableNames, ...
            corrMatrix, 'Colormap', parula, 'CellLabelFormat', '%0.2f');
```
**خط ۳۰-۳۱:** 
- `heatmap` یک نقشه حرارتی می‌کشد.
- دو آرگومان اول نام ستون‌ها برای محور افقی و عمودی هستند.
- `corrMatrix` داده‌های همبستگی است.
- `'Colormap', parula` رنگ‌بندی را روی `parula` تنظیم می‌کند.
- `'CellLabelFormat', '%0.2f'` اعداد داخل خانه‌ها را با دو رقم اعشار نمایش می‌دهد.

```matlab
    title('نقشه حرارتی همبستگی بین ویژگی‌ها');
```
**خط ۳۲:** عنوان نمودار را تنظیم می‌کند.

```matlab
    drawnow;
```
**خط ۳۳:** نمودار را فوراً رسم می‌کند.

```matlab
    disp('✅ نمودارهای تحلیل داده رسم شدند.');
    disp(' ');
```
**خط ۳۵-۳۶:** پیغام موفقیت و یک خط خالی برای خوانایی بهتر.

```matlab
end
```
**خط ۳۷:** پایان تابع.

---

## چرا این تابع مهم است؟

قبل از این‌که سراغ «ساخت مدل» برویم، باید:

- بدانیم داده‌ها چطور توزیع شده‌اند (آیا مقدارهای خیلی عجیب داریم یا نه؟).  
- بفهمیم کدام ویژگی‌ها ممکن است اطلاعات مشابه بدهند (همبستگی خیلی بالا).  
- حدس بزنیم کدام ویژگی‌ها احتمالاً برای پیش‌بینی دیابت مهم‌تر هستند.

تابع `analyze_data.m` با همین هدف ساخته شده تا یک نگاه شهودی و سریع به دیتاست داشته باشیم.
